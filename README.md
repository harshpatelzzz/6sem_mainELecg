# Adaptive Personalized ECG Intelligence System\n\nSee architecture, setup, training, API, and testing instructions for this full-stack research system.\n\n## Overview\nMulti-scale CNN + BiLSTM + memory + GNN + uncertainty fusion for personalized ECG risk modeling.\n\n## Prerequisites\nPython 3.11, Docker, Node 20\n\n## Quickstart\n`cp .env.example .env && docker compose up --build`\n\n## API\n`POST /api/v1/analyze`, `GET /api/v1/patient/{id}`, `GET /api/v1/risk/{id}/history`, etc.\n\n## Tests\n`pytest tests/ --cov=src --cov-report=html`\n